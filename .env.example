# 环境变量配置示例

# 必需：从浏览器复制的完整 Cookie
# 在浏览器开发者工具中，找到 /api/chat/ 请求，复制 Cookie 请求头
YUANBAO_COOKIE="hy_token=xxx; hy_user=xxx; ..."

# 可选：Agent ID（默认: naQivTmsDa）
YUANBAO_AGENT_ID="naQivTmsDa"

# 可选：服务端口（默认: 3000）
PORT=3000

# 注意：不再需要 YUANBAO_CONVERSATION_ID，每次请求自动创建临时会话
